# 2026-05-27 GPUShare RTX 3090 i-1

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-05-27 |
| 服务器别名 | `gpushare-rtx3090-i1` |
| 平台 | GPUShare |
| 用途 | final / benchmarking / SHAP / soil-only screening |
| 项目目录 | `/hy-tmp/sers_ml_20260526/core_subject` |
| 本地 Git commit | `d4187ff7a36e5a9b2f8f4780e895fcb301deaa88` |
| 远程 Git commit | `N/A`（远端为 tarball snapshot，无 `.git` 元数据） |

## 2. 硬件与系统

| 项目 | 内容 |
| --- | --- |
| 系统镜像 | Ubuntu 22.04.3 LTS |
| GPU | NVIDIA GeForce RTX 3090 |
| GPU 显存 | 24576 MiB |
| NVIDIA Driver | 535.216.03 |
| CPU | AMD EPYC 7542 32-Core Processor |
| CPU 核数 | 128 logical CPUs / 64 physical cores |
| RAM | 2.0 TiB |
| 磁盘 | `/` overlay 30G；`/hy-tmp` 50G |

## 3. Python 与关键包

| 包/运行时 | 版本 |
| --- | --- |
| Python | 3.11.10 |
| PyTorch | `2.4.0+cu121` |
| torch CUDA build | 12.1 |
| CUDA toolkit/runtime | 12.2（`nvidia-smi` 报告） |
| cuDNN | N/A（未单独查询） |
| XGBoost | N/A（本轮正式实验未安装） |
| NumPy | 2.1.2 |
| pandas | 2.3.3 |
| scikit-learn | 1.7.2 |
| SciPy | 1.16.3 |
| SHAP/Captum | N/A（本轮正式实验未安装；`run_paper_shap.py --match-only` 不依赖 `shap`） |

环境快照文件：

- `pip freeze`：`/hy-tmp/sers_ml_20260526/logs/env_snapshot_20260527_002100.txt`
- GPU/CPU/RAM 快照：同上
- 命令清单：
  - `python3 -m pip install --prefer-binary --only-binary=:all: pandas==2.3.3 scipy==1.16.3 scikit-learn==1.7.2 joblib==1.5.3 threadpoolctl==3.6.0 pytz==2026.2 tzdata==2026.2`
  - `python3 scripts/analysis/run_paper_shap.py --model ExtraTrees --variant p1 --match-only`
  - `python3 scripts/analysis/run_shap_feature_selection.py`
  - `python3 scripts/analysis/run_soil_only_cv.py --n-permutations 10000`

## 4. 运行范围

- Stage：`paper_main`
- 模型：`ExtraTrees`
- 预处理：`p1`
- 任务：SHAP 匹配覆盖 P1/P2/P3/G2/G3；SHAP-guided FS 覆盖 P1/P2/P3/G2/G3；soil-only CV 覆盖 P1/P2/P3 presence
- split/fold：论文随机 5-fold split；soil-only 5-fold same-matrix CV
- seed：`42`（脚本固定种子）
- 命令：
  - `python3 scripts/analysis/run_paper_shap.py --model ExtraTrees --variant p1 --match-only`
  - `python3 scripts/analysis/run_shap_feature_selection.py`
  - `python3 scripts/analysis/run_soil_only_cv.py --n-permutations 10000`

## 5. 工程调整

- `scripts/analysis/run_paper_shap.py` 新增 `--match-only` 模式，用于只刷新峰位匹配，不重算 TreeSHAP。
- `scripts/analysis/run_soil_only_cv.py` 修正 blank 缓存加载逻辑：优先读取已存在的 `X_blanks_p1.npy`，避免在远端尝试重建本地原始 CSV。
- 远端安装改为最小必需依赖集，避免 `captum` 与 `shap` 的 `numpy` 版本冲突。

## 6. 产物回传

| 项目 | 内容 |
| --- | --- |
| 本地 stage 目录 | `D:\通过拉曼光谱预测物及其浓度\附\服务器环境记录\runs\20260527_gpushare_3090_sers_ml\extracted` |
| `logs/` 已回传 | yes |
| `timings/` 已回传 | no |
| `run_metadata/` 已回传 | yes |
| stage index 已校验 | yes |
| 文档/记忆已更新 | yes |

## 7. 备注

- 远端 tarball 归档：`/hy-tmp/sers_ml_20260526/sers_ml_outputs_20260527_002100.tar.gz`
- 本记录不保存 SSH 用户名、密码或任何其他远程凭据。
