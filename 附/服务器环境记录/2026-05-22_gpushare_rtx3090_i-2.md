# 2026-05-22 GPUShare RTX 3090 i-2

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-05-22 |
| 服务器别名 | `gpushare-rtx3090-i2` |
| 平台 | GPUShare |
| 用途 | paper-main random-CV 远程 smoke/profile、first-round classical fast screening、完整 12×6 benchmark 补全 |
| 项目目录 | `/hy-tmp/SERS-multicomponent-analysis` |
| 初始部署 Git commit | `60ab0aa` |
| first-round benchmark 远程 Git commit | `e5b0aa713ddfbb1a2341a8298d3c1ad7e4260886` |
| first-round 本地回传固化 commit | `75de4ff` |
| complete benchmark 本地回传固化 commit | `ba0d2b2` |

本记录不保存 SSH 用户名、密码或其它远程凭据。

## 2. 硬件与系统

| 项目 | 实测/已知内容 |
| --- | --- |
| 系统 | Linux，`uname` 内核 `5.4.0-174-generic` |
| GPU | NVIDIA GeForce RTX 3090 |
| GPU 显存 | 24576 MiB |
| NVIDIA Driver | 535.161.08 |
| CPU 容器观测 | Intel Xeon E5-2682 v4 @ 2.50GHz |
| CPU 线程观测 | `nproc` 返回 64；平台购买口径曾显示 12 核，运行决策以 smoke/profile 实测为准 |
| RAM 容器观测 | 125 GiB total，环境检查时约 102 GiB available |
| 根盘 | overlay 30G |
| 项目盘 | `/hy-tmp` 50G NVMe |

## 3. Python 与关键包

远程镜像初始状态：

| 包/运行时 | 版本 |
| --- | --- |
| Python | 3.11.10 |
| PyTorch | `2.4.0+cu121` |
| torch CUDA build | 12.1 |
| CUDA 可用 | yes，检测到 RTX 3090 |

项目部署后执行：

```bash
python3 -m pip install -r requirements.txt
```

安装后关键包观测：

| 包 | 版本 |
| --- | --- |
| NumPy | 2.1.2 |
| pandas | 3.0.3 |
| scikit-learn | 1.8.0 |
| SciPy | 1.17.1 |
| XGBoost | 3.2.0 |
| SHAP | 0.51.0 |
| Captum | 0.9.0 |

完整 `pip freeze` 会随 stage `run_metadata/` 回传。

## 4. 已做工程调整

- `60ab0aa` 为服务器跑批加入 fold 级 `FitSeconds` / `PredictSeconds` 指标。
- 活跃单任务 DL wrapper 使用 pinned memory 和 non-blocking CUDA transfer。
- 项目 clone 到 `/hy-tmp`，避免把实验输出写到较小的根盘。
- 远程运行按 stage 写入 `logs/`、`timings/`、`run_metadata/`，不得只回传 best-score 表。

## 5. 远程 smoke/profile

| 项目 | 内容 |
| --- | --- |
| Stage | `paper_main/benchmark_random_cv` |
| Run ID | `smoke_3090_20260522` |
| 模型 | RF, XGBoost, 1D-CNN, Spectrum-KAN |
| 预处理 | p1 |
| split | `cv_split_random_5fold.csv` |
| 日志 | `data/pure63_mainline/models/paper_main/benchmark_random_cv/logs/smoke_3090_20260522.log` |
| metadata | `data/pure63_mainline/models/paper_main/benchmark_random_cv/run_metadata/` |

启动命令：

```bash
python3 -u scripts/analysis/run_random_benchmark.py \
  --models RF XGBoost 1D-CNN Spectrum-KAN \
  --variants p1 \
  --strict-models
```

smoke 已完成并回传。本阶段的主要作用是确认远端环境、XGBoost、基础 DL wrapper、日志/metadata 回传链路可用。

## 6. 当前回传状态

### smoke/profile

- 2026-05-22 已将 `p1` smoke/profile bundle 回传到本地 `core subject/data/pure63_mainline/models/paper_main/benchmark_random_cv/`。
- 本地已核对 `cv_results_detail_p1.csv` 有 120 条 fold 明细，覆盖 6 个任务、4 个模型、5 个 fold。
- 回传包包含 detail/summary/prediction CSV、benchmark index、smoke 日志、command manifest、data check、environment snapshot 和 `pip freeze`。

### classical screening and completion

- smoke 结束后，远端曾自动开始传统模型全筛；初始 LDA 路径在 raw/P1 上过慢，因此中止并保留日志。
- LDA solver profile 已写入 `timings/lda_solver_profile_raw_p1task_fold0_20260522.csv`。raw/P1 的一个 fold 实测约为 `svd=115.8s`、`lsqr=None=120.8s`、`lsqr+auto=133.3s`。
- Run `classical_fast_no_lda_3090_20260522` 已完成并回传：RF、ExtraTrees、HistGradientBoosting、XGBoost、SVM、KNN、PLS-DA，预处理为 `raw,p1,p2,p3,p4,p5`。
- 本地校验通过：`raw,p2,p3,p4,p5` 各 210 条 detail 与 42 条 summary；`p1` 为 270 条 detail 与 54 条 summary，因为其中合并了 1D-CNN 与 Spectrum-KAN smoke 行。
- 最终索引文件已回传：`benchmark_best_by_task.csv`、`benchmark_full_matrix.csv`、`benchmark_source_manifest.csv`、`benchmark_source_model_mean.csv`、`benchmark_source_preprocess_mean.csv`。
- 当前 first-round best-by-task macro-F1：P1 KNN/p1 0.996，P2 RF/p1 0.985，P3 ExtraTrees/p4 0.994，G1 ExtraTrees/p2 1.000，G2 ExtraTrees/p1 0.966，G3 SVM/p1 0.981。
- Run `benchmark_completion_3090_20260522` 已完成并回传：补齐 1D-CNN/Spectrum-KAN 的缺失预处理，新增 1D-ResNet/KAN-CNN 全预处理，并补齐 LDA 全预处理。
- 本地完整校验通过：`raw,p1,p2,p3,p4,p5` 各 360 条 detail、72 条 summary、56,184 条 prediction，覆盖 12 个模型、6 个任务、5 个 fold。
- 完整索引：`benchmark_full_matrix.csv` 为 432 条 summary，`benchmark_best_by_task.csv` 为 6 条任务最优。
- 完整 benchmark 的 best-by-task 与 first-round 相同：P1 KNN/p1 0.996，P2 RF/p1 0.985，P3 ExtraTrees/p4 0.994，G1 ExtraTrees/p2 1.000，G2 ExtraTrees/p1 0.966，G3 SVM/p1 0.981。
- 完整矩阵均值显示 ExtraTrees、XGBoost、RF 领先；DL/KAN 模型作为 benchmark 对照证据保留。
- 注：虽然早期 LDA profile 很慢，但最终 completion wrapper 中 LDA 完成很快并已进入完整矩阵；后续引用以完整回传结果为准。
