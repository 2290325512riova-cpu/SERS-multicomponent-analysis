# Agent Entry Point

This repository keeps the full agent protocol in:

```text
project_memory/AGENTS.md
```

Before planning, editing, running experiments, or changing project direction, read that file first, then follow its required startup context.
